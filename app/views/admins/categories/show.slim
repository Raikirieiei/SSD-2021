h1 = @category.tag
div = link_to 'Back', admins_categories_path
hr
h2 = 'description'
div = @category.description
hr
h2 = 'id list'
div
    - @category.products.each do |f|
        div = link_to "#{f.title}" , admins_product_path(f)
